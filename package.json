{
  "private": true,
  "workspaces": ["packages/ousia"],
  "scripts": {
    "smoke:install": "npm --prefix packages/ousia run build && node smoke/install-smoke.mjs",
    "release:check": "node smoke/release-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3"
  }
}
