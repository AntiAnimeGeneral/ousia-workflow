---
applyTo: "**"
description: "设计任务规范：系统设计、重构方案、接口/数据模型设计、候选方案、边界、迁移、验证和实施计划。"
---

# 设计任务规范

当用户要求系统设计、模块设计、架构调整、重构方案、接口设计、数据模型设计、边界重组、技术选型，或任务需要先给方案再决定是否实现时，按设计任务处理。

## 设计校验

- 沿用现有模式前，按 `.github/instructions/ousia-development-entry.instructions.md` 判断它是稳定约束还是历史偶然；填不出“这个模式仍然成立，因为……”时，不要沿用。
- 关键实现完成后检查：状态归谁拥有？谁负责编排？谁负责副作用？不变量在哪里建立？行为在哪里测试？
- 如果任何答案不确定，或分散在多个地方，设计通常还没完成。

## 设计任务必须覆盖

- 背景与约束。
- 目标与非目标。
- 现状中哪些结构应继承、演进或停止模仿，以及原因。
- 至少两类候选方案；在相关时比较现有模式、成熟库/组件和自定义实现。
- 推荐方案与取舍理由。
- 模块边界与依赖方向。
- 状态所有权、数据流、副作用边界，以及校验/归一化所在层。
- 第一个可实施的纵向切片，以及边界整理如何服务这个切片。
- 按任务规模说明测试策略、兼容性、迁移路径、回滚方式、验证方式和演进风险。

完整 proposal packet、implementation handoff 和 review focus 的输出格式由 [SKILL.md](../skills/architecture-planner/SKILL.md) 声明；本 instruction 只规定设计任务必须证明哪些语义。

## 避免

- 只复述现有结构。
- 只给单一路径。
- 只画形式分层却不说明状态归属和依赖方向。
- 只输出边界清理、模块重排或命名修正，却没有说明这些调整服务于哪个可验证纵向切片。
- 用抽象名词包装架构感却不能解释职责和演进路径。
